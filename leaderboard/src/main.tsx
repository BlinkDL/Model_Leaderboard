import React from 'react';
import ReactDOM from 'react-dom/client';
import LLMBoard from './TS';

ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
  <React.StrictMode>
    <LLMBoard />
  </React.StrictMode>,
);
